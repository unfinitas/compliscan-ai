export enum RequestEnum {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
  PATCH = 'PATCH',
}

export enum AuthApiErrorEnum {
  OK = 'OK',
  FORBIDDEN = 'FORBIDDEN',
  UNAUTHORIZED = 'UNAUTHORIZED',
  CONFLICT = 'CONFLICT',
  INVALID_TOKEN = 'INVALID_TOKEN',
  UNKNOWN = 'UNKNOWN',
}

